{
  "name": "ai-core",
  "private": true,
  "version": "0.0.1",
  "description": "Morning Coffee Labs AI Core (local-first agent platform).",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "node --enable-source-maps dist/agent-cli/index.js --help",
    "start": "node dist/agent-cli/index.js --help",
    "ai": "node dist/agent-cli/index.js",
    "ai:help": "node dist/agent-cli/index.js --help",
    "clean": "rimraf dist .cache .tmp"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "rimraf": "^6.0.1",
    "typescript": "^5.6.0"
  }
}
