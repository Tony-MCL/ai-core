# vscode-extension

Second deliverable: VS Code UI for ai-core.
- Side panel chat
- Diff preview
- Apply patch button
- Verify status

The extension should call a local agent server (localhost) to keep the UI thin.
