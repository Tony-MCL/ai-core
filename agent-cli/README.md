# agent-cli

First deliverable: a local CLI that can:
- index a repo
- search + read files
- propose a patch
- apply patch (confirm)
- run verify commands

The CLI will later share the same core runtime as the VS Code extension backend.
